apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stateforge-data
  labels:
    app.kubernetes.io/name: stateforge
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
