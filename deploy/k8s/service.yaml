apiVersion: v1
kind: Service
metadata:
  name: stateforge-kestrel
  labels:
    app.kubernetes.io/name: stateforge
    app.kubernetes.io/component: kestrel
spec:
  selector:
    app.kubernetes.io/name: stateforge
    app.kubernetes.io/component: kestrel
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
